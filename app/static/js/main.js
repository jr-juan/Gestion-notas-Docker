// Punto de partida para el compañero 3/4:
// aquí van los fetch() hacia las rutas Flask que expongan el CRUD
// (por ejemplo /api/notas, /api/estudiantes, /api/materias)

document.addEventListener("DOMContentLoaded", () => {
    console.log("Gestión de Notas - frontend listo");
});
